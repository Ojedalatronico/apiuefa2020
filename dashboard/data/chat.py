"""import plotly.io as pio
import plotly.graph_objs as go

def radar_plot(data):
    theta = [s["name"].title() for s in stats]
    fig=go.Figure()
    for s in data:
        fig.add_trace(go.Scatterpolar(
                                r=s[],
                                theta=theta,
                                name=p["Name"],
                                fill="toself"
        ))"""